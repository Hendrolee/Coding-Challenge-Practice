const paymentsData = require('../data/payments.json')
const parsers = require('../utils/parsers')
const User = require('../src/user.js')
const users = []
// check whether payments JSON is valid and complete
// check whether User class is creating the correct instance
// check users array is storing the correct user data / without duplication


describe('parsePayment', () => {
  it('payments JSON is valid and producing complete data', () => {
  })
})